spring:
  datasource:
    url: 'jdbc:postgresql://pg-db:5432/fhir'
    username: fhir
    password: fhir
    driverClassName: org.postgresql.Driver
  jpa:
    properties:
      hibernate.dialect: ca.uhn.fhir.jpa.model.dialect.HapiFhirPostgresDialect
      hibernate.search.enabled: true